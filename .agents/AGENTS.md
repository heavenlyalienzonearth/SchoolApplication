# Vidyankuram School Application Developer Rules

## Git Push Permission Constraint
- **CRITICAL**: The agent MUST ask for explicit user permission in the chat before running any `git push` command on the repository. Do not run `git push` without user approval.
